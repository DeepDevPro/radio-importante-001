files:
  "/etc/nginx/conf.d/proxy_upload.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 150M;

commands:
  01_restart_nginx:
    command: "systemctl restart nginx"

